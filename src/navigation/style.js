import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  textoVoltar: {
    fontSize: 12,
    fontWeight: 'bold',
    marginBottom: 2,
  },
  btnVoltar: {
    marginLeft: 10,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
